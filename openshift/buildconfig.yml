kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "video-copy"
spec:
  source:
    git:
      uri: "https://github.com/ivid-123/video-tool.git"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfilePath: Jenkinsfile
